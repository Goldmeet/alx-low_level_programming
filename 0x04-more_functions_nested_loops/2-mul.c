#include "main.h"
/**
 *mul- check the multiplies two digits
 *@a: s an integer.
 *@b: is an integer
 *Return: Always return a value.
 */
int mul(int a, int b)
{
  int result;
  result = a * b;
  return (result);

}
