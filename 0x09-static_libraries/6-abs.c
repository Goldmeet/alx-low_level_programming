#include "main.h"
/**
 *_abs- print positive of any value
 *@n:is of integer type
 *
 *Return: Always return n
 */
int _abs(int n)
{
    
    if (n < 0)
    {
         n *= -1;
        return (n);
    }
    else 
    
        return (n); 
}
